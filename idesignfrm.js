function changeLabelEventOnControl(label_val, elemType_val){
    // this code was added for giving a hook that will work,
    // when we change some value in a control, 
    //see textarea for this it has been called there


    // eg code to check added below
    // window.alert("label :", label_val , "elemType_val :" , elemType_val );
}